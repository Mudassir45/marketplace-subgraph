{
  "name": "Marketplace-Graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ mudassir/Marketplace-Graph",
    "create-local": "graph create --node http://localhost:8020/ mudassir/Marketplace-Graph",
    "remove-local": "graph remove --node http://localhost:8020/ mudassir/Marketplace-Graph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mudassir/Marketplace-Graph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
